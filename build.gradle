plugins {
    // Declared here so subprojects can apply it with `apply plugin: 'fabric-loom'`
    // inside gradle/fabric-common.gradle without re-declaring the version.
    id 'fabric-loom' version '1.15-SNAPSHOT' apply false
}
